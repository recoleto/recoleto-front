import { MainLayout } from "@/components/main-layout";
import { Text } from "react-native";

export default function AdminCompanies() {
    return (
        <MainLayout>
            <Text>Admin Companies</Text>
        </MainLayout>
    )
}